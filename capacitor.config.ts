import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.privatefleetservices.crm',
  appName: 'Private Fleet',
  webDir: 'dist'
};

export default config;
